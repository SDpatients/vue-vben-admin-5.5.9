# 公告管理文件上传调试指南

## 📋 调试代码说明

为了定位文件上传问题，已在关键位置添加了详细的调试日志。

---

## 🔍 调试日志位置

### 1. **公告列表页面** (`announcement-list/index.vue`)

#### 文件变化监听
```typescript
// 本地文件变化
handleLocalFilesChange(files)
// 输出：📁 [调试] 本地文件变化

// 手机文件上传
handleMobileFilesUploaded(files)
// 输出：📱 [调试] 手机上传了文件
```

#### 提交流程
```typescript
submitPublishForm()
// 输出：
// 🚀 [调试] 开始提交公告表单
// 📋 [调试] 当前表单数据
// 📂 [调试] 当前附件总数
// 📂 [调试] 附件详情
// 🔍 [调试] 检查文件
// 📊 [调试] 文件分类结果
```

---

### 2. **文件上传组件** (`FileUpload.vue`)

#### 手机上传轮询
```typescript
pollTempFiles()
// 输出：
// 📱 [FileUpload 调试] 手机上传了新文件
// 📂 [FileUpload 调试] 添加手机文件到本地列表
// 📤 [FileUpload 调试] 触发 local-files-change 事件
// 📤 [FileUpload 调试] 触发 mobile-files-uploaded 事件
```

#### 本地文件上传
```typescript
uploadLocalFiles(bizId)
// 输出：
// 📤 [FileUpload 调试] uploadLocalFiles 被调用
// 📂 [FileUpload 调试] 准备上传的文件列表
// ⏳ [FileUpload 调试] 开始上传文件
// 📥 [FileUpload 调试] 文件上传响应
// 📊 [FileUpload 调试] 上传结果统计
```

#### 手机文件转移
```typescript
transferMobileFiles(bizId)
// 输出：
// 转移临时文件失败：缺少必要参数（如果有问题）
// 成功转移 X 个文件到业务（成功时）
```

---

## 🛠️ 调试步骤

### 步骤 1：打开浏览器控制台
1. 按 F12 打开开发者工具
2. 切换到 Console 标签
3. 清空控制台（点击🚫图标）

### 步骤 2：测试本地文件上传

#### 操作流程：
1. 点击"发布公告"按钮
2. 选择案件
3. 填写标题、内容
4. 点击"上传文件"按钮
5. 选择本地文件

#### 期望看到的日志：
```
📁 [调试] 本地文件变化
  - filesCount: 1
  - files: [{ id: "local-xxx", name: "test.pdf", hasFileObject: true }]

🚀 [调试] 开始提交公告表单
📋 [调试] 当前表单数据
📂 [调试] 当前附件总数：1
📂 [调试] 附件详情
🔍 [调试] 检查文件 test.pdf: id=local-xxx, isLocal=true
📊 [调试] 文件分类结果
  - mobileFilesCount: 0
  - localFilesCount: 1
⏳ [调试] 开始调用 createAnnouncementApi
✅ [调试] 公告创建成功，announcementId: 123
💻 [调试] 检测到本地文件，准备上传
📤 [调试] 准备上传的本地文件
⏳ [调试] 开始调用 uploadAnnouncementAttachmentsApi
📥 [调试] uploadAnnouncementAttachmentsApi 响应
✅ [调试] 本地文件上传成功
```

#### 可能的问题：
1. **没有看到 `📁 [调试] 本地文件变化`**
   - 问题：FileUpload 组件没有触发事件
   - 检查：FileUpload 组件的事件绑定

2. **看到 `hasFileObject: false`**
   - 问题：文件对象丢失
   - 检查：handleLocalFilesChange 方法

3. **看到 `localFilesCount: 0`**
   - 问题：文件没有被正确收集
   - 检查：FileUpload 组件的 localFiles 数组

---

### 步骤 3：测试手机扫码上传

#### 操作流程：
1. 点击"发布公告"按钮
2. 选择案件
3. 填写标题、内容
4. 点击"手机扫码上传"按钮
5. 手机扫码并上传文件

#### 期望看到的日志：
```
📱 [FileUpload 调试] 手机上传了新文件
  - diffCount: 1
  - newFiles: [{ id: 456, name: "photo.jpg", size: 102400 }]

📂 [FileUpload 调试] 添加手机文件到本地列表
  - filesCount: 1
  - files: [{ id: "mobile-456", name: "photo.jpg", hasFileObject: false }]

📤 [FileUpload 调试] 触发 local-files-change 事件
✅ [FileUpload 调试] local-files-change 事件已触发

📤 [FileUpload 调试] 触发 mobile-files-uploaded 事件
✅ [FileUpload 调试] mobile-files-uploaded 事件已触发

📱 [调试] 手机上传了文件
  - filesCount: 1

🚀 [调试] 开始提交公告表单
📂 [调试] 当前附件总数：1
🔍 [调试] 检查文件 photo.jpg: id=mobile-456, isMobile=true
📊 [调试] 文件分类结果
  - mobileFilesCount: 1
  - localFilesCount: 0
📱 [调试] 检测到手机上传文件，准备转移
⏳ [调试] 开始调用 transferMobileFiles, announcementId: 123
📥 [调试] transferMobileFiles 完成，结果：[...]
✅ [调试] 手机文件转移完成
```

#### 可能的问题：
1. **没有看到 `📱 [FileUpload 调试] 手机上传了新文件`**
   - 问题：手机上传后轮询没有检测到
   - 检查：轮询间隔、Token 是否有效

2. **没有看到 `📤 [FileUpload 调试] 触发 local-files-change 事件`**
   - 问题：事件没有触发
   - 检查：isLocalMode 是否为 true

3. **看到 `hasFileObject: false`（这是正常的）**
   - 说明：手机上传的文件在本地只有元数据，实际文件在服务器

4. **看到 `fileUploadRef 不存在，无法转移文件`**
   - 问题：ref 引用丢失
   - 检查：FileUpload 组件的 ref 绑定

---

## 🐛 常见问题排查

### 问题 1：点击发布后没有任何文件上传日志

**可能原因：**
1. 文件没有被添加到表单数据
2. 文件分类逻辑有误

**排查步骤：**
1. 查看 `📂 [调试] 当前附件总数` 是否为 0
2. 查看 `📂 [调试] 附件详情` 中的文件列表
3. 检查 `handleLocalFilesChange` 是否被调用

---

### 问题 2：本地文件上传失败

**可能原因：**
1. 文件对象为空
2. API 调用失败
3. 文件大小超限

**排查步骤：**
1. 查看 `📂 [调试] 准备上传的文件列表` 中的 `hasFileObject`
2. 查看 `📥 [调试] 文件上传响应` 检查 API 返回
3. 查看文件大小是否超过 50MB

---

### 问题 3：手机上传文件没有转移

**可能原因：**
1. fileUploadRef 引用丢失
2. 文件 ID 格式不正确
3. Token 过期或无效

**排查步骤：**
1. 查看是否有 `fileUploadRef 不存在` 错误
2. 查看文件 ID 是否以 `mobile-` 开头
3. 查看 Token 是否有效

---

## 📊 关键数据结构

### LocalFileItem 结构
```typescript
{
  file: File,              // 本地文件对象（手机上传为空）
  id: string,              // 文件 ID（mobile-xxx 或 local-xxx）
  originalFileName: string, // 原始文件名
  fileSize: number,        // 文件大小（字节）
  fileExtension: string,   // 文件扩展名
  mimeType: string,        // MIME 类型
  uploadTime: string,      // 上传时间
}
```

### 文件 ID 规则
- **手机上传文件**：`id = "mobile-" + tempFile.id`
- **本地文件**：`id = "local-" + 随机字符串`
- **已有文件**：`id = 数字 ID`（字符串格式）

---

## 🎯 验证成功的标志

### 本地文件上传成功：
1. ✅ 看到 `📁 [调试] 本地文件变化`
2. ✅ 看到 `hasFileObject: true`
3. ✅ 看到 `⏳ [调试] 开始调用 uploadAnnouncementAttachmentsApi`
4. ✅ 看到 `✅ [调试] 本地文件上传成功`

### 手机文件上传成功：
1. ✅ 看到 `📱 [FileUpload 调试] 手机上传了新文件`
2. ✅ 看到 `📤 [FileUpload 调试] 触发 local-files-change 事件`
3. ✅ 看到 `📱 [调试] 手机上传了文件`
4. ✅ 看到 `📥 [调试] transferMobileFiles 完成`

---

## 📝 调试日志符号说明

| 符号 | 含义 |
|------|------|
| 🚀 | 流程开始 |
| 📋 | 数据展示 |
| 📂 | 文件信息 |
| 🔍 | 检查/过滤 |
| 📊 | 统计信息 |
| ⏳ | API 调用中 |
| 📤 | 发送请求 |
| 📥 | 接收响应 |
| ✅ | 成功 |
| ❌ | 失败/错误 |
| ⚠️ | 警告 |
| 📱 | 手机相关 |
| 💻 | 本地文件相关 |
| 🏁 | 流程结束 |

---

## 🔧 如何提供调试信息

当文件上传出现问题时，请：

1. **清空控制台**
2. **重新操作一遍**
3. **复制所有带有 `[调试]` 或 `[FileUpload 调试]` 的日志**
4. **提供完整的日志序列**

这样可以帮助快速定位问题所在！
