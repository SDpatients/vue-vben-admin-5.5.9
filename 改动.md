核心结论：API 路径和参数名均无变化，只有返回值和搜索行为有变化
一、数据脱敏 → 返回值格式变化 ⚠️ 需适配
以下 API 返回的 JSON 字段值发生变化（字段名不变）：

1. 用户列表/详情
API	字段	原来	现在
GET /system/user/list	mobile	"13812345678"	"138****5678"
GET /system/user/list	phone	"13812345678"	"138****5678"
GET /system/user/{id}	mobile	"13812345678"	"138****5678"
⚠️ 注意：GET /system/user/me（当前用户自己的信息）不脱敏，mobile 和 phone 返回完整值。

2. 债权人列表/详情
API	字段	原来	现在
GET /creditor/info/*	idNumber	"330502199001011234"	"3305**********1234"
GET /creditor/info/*	contactPhone	"13812345678"	"138****5678"
GET /creditor/info/simple/*	idNumber	"330502199001011234"	"3305**********1234"
GET /creditor/claim-query/*	idNumber	"330502199001011234"	"3305**********1234"
GET /creditor/claim-query/*	contactPhone	"13812345678"	"138****5678"
GET /creditor/claim-query/*	creditorBankAccount	"6222021234567890"	"6222 **** **** 7890"
💡 前端对策：如果有"点击查看完整号码"功能，需要新增一个请求完整明文的后端接口（加二次验证），当前脱敏接口用于列表展示。

3. 债权申报详情（多阶段）
API	字段	原来	现在
GET /creditor/claim-stages/*	agentPhone	"13812345678"	"138****5678"
GET /creditor/claim-stages/*	agentIdCard	"330502199001011234"	"3305**********1234"
GET /creditor/claim-stages/*	creditorBankAccount	"6222021234567890"	"6222 **** **** 7890"
4. 银行账户列表
API	字段	原来	现在
GET /fund/account/list	accountNumber	"6222021234567890"	"6222 **** **** 7890"
二、搜索行为变化 ⚠️ 需适配
身份证号搜索：模糊 → 精确
API	参数	原来行为	现在行为
GET /creditor/info/search	idNumber	LIKE '%3305%' 模糊匹配	= '330502199001011234' 精确匹配
⚠️ 前端对策：用户输入部分身份证号将无法匹配，必须输入完整号码。建议前端表单上增加提示：

PlainText



请输入完整身份证号进行精确查询
用户搜索：手机号从模糊搜索中移除
API	原来搜索字段	现在搜索字段
GET /system/user/search?keyword=xxx	用户名 + 姓名 + 手机号	用户名 + 姓名（手机号不再参与模糊匹配）
💡 如需按手机号精确查找用户，使用 GET /system/user/mobile/{mobile} 接口。

三、密码规则变化 ⚠️ 需适配
项目	原来	现在
最小长度	6 位	6 位（不变）
复杂度	无要求	大写 + 小写 + 数字 + 特殊符号，至少 3 种
错误提示	无	"密码必须包含大写字母、小写字母、数字、特殊符号中的至少3种"
⚠️ 前端对策：

注册页/修改密码页增加实时密码强度校验
建议显示进度条：满足 3 种 → 绿色 ✓
错误时展示具体提示文案
四、审计日志变化 ⚠️ 需调整
变更	影响
DELETE /system/audit/{id}	已移除（返回 404）
DELETE /system/audit/clean	已移除（返回 404）
GET /system/audit/* 查询类	不变
⚠️ 前端对策：移除审计日志页面的"删除"和"批量清理"按钮。

五、License 无效时的降级行为
当 License 过期或机器码不匹配时：

场景	行为
加密字段（身份证/手机号/银行账号）	返回值变成 "***"
非加密字段	正常返回
写操作	明文写入（不加密）
💡 前端对策：检测到敏感字段全为 "***" 时，弹出提示：「许可证已过期或服务器不匹配，敏感数据无法显示，请联系管理员续期」

六、不变的部分 ✅ 无需调整
类别	说明
API 路径	所有路径不变
请求参数名	所有参数名不变
请求参数类型	类型不变（String/Integer/Long 等）
非脱敏字段	所有非敏感字段返回值不变
状态码	成功/失败状态码不变
分页格式	PageResult 结构不变
📊 前端改动检查清单
序号	模块	改动内容	优先级
1	用户列表	mobile/phone 展示为脱敏格式 138****5678	🔴 高
2	债权人列表	idNumber/contactPhone 展示为脱敏格式	🔴 高
3	债权申报详情	agentPhone/agentIdCard/bankAccount 脱敏	🔴 高
4	银行账户列表	accountNumber 脱敏展示	🔴 高
5	身份证号搜索	改为精确匹配，添加提示文案	🔴 高
6	用户搜索	手机号从关键词搜索中移除	🟡 中
7	注册/修改密码	增加密码强度实时校验（3/4 规则）	🔴 高
8	审计日志	移除「删除」和「清理」按钮	🟡 中
9	License 过期提示	检测 "***" 时弹出续期提示	🟢 低
10	查看完整信息	如需"点击查看完整号码"，需新增后端接口	🟢 按需
