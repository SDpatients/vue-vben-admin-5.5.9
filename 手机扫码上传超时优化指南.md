# 手机扫码上传超时优化指南

## ✅ 已完成的优化

### 1️⃣ IP 检测超时（2 秒）

**位置**: `FileUpload.vue` - `detectLocalIP()`

**优化内容**:
- ⏱️ 超时时间：**2 秒**
- 🔄 失败策略：超时后自动使用后备 IP
- 💬 友好提示：显示"IP 检测失败，将使用默认配置，手机上传可能无法使用"

**关键代码**:
```typescript
// 设置超时：2 秒
const timeoutId = setTimeout(() => {
  if (!foundIP) {
    console.log('[IP 检测] ⏰ WebRTC 检测超时（2 秒）');
    rtc.close();
    resolve(useFallbackIP()); // 不 reject，使用后备方法
  }
}, 2000);

// 错误处理
rtc.onicecandidateerror = (error) => {
  console.error('[IP 检测] ❌ WebRTC 错误:', error);
  clearTimeout(timeoutId);
  resolve(useFallbackIP());
};
```

---

### 2️⃣ 创建 Token 超时（10 秒）

**位置**: `FileUpload.vue` - `openMobileUploadDialog()`

**优化内容**:
- ⏱️ 超时时间：**10 秒**
- 🔄 失败策略：reject Promise，显示错误提示
- 💬 友好提示：
  - 加载中："正在准备上传环境，请稍候..."
  - 成功："上传环境准备成功"
  - 超时："操作超时，请检查网络连接后重试"
  - 失败："创建上传 Token 失败，请稍后重试"

**关键代码**:
```typescript
const loadingMsg = ElMessage({
  message: '正在准备上传环境，请稍候...',
  type: 'info',
  duration: 0, // 不自动关闭
});

// 设置超时：10 秒
const timeoutPromise = new Promise((_, reject) => {
  setTimeout(() => {
    reject(new Error('创建上传 Token 超时（10 秒）'));
  }, 10000);
});

const tokenResponse = await Promise.race([tokenPromise, timeoutPromise]);

// 错误处理
let errorMsg = '打开手机上传功能失败';
if (error.message?.includes('超时')) {
  errorMsg = '操作超时，请检查网络连接后重试';
}
ElMessage.error(errorMsg);
```

---

### 3️⃣ 文件轮询检测（最大 30 分钟）

**位置**: `FileUpload.vue` - `startTempFilePolling()` & `pollTempFiles()`

**当前实现**:
- ⏱️ 轮询间隔：**3 秒**
- 🔄 停止条件：
  - 用户关闭二维码弹窗
  - Token 过期（30 分钟）
- 💬 友好提示：
  - "手机上传了 X 个新文件"
  - "二维码有效期 30 分钟，过期后请重新打开"

**优化建议**（可选）:
```typescript
// 添加最大轮询次数限制
const MAX_POLL_COUNT = 600; // 3 秒 * 600 = 30 分钟
let pollCount = 0;

const pollTempFiles = async () => {
  if (!currentTempToken.value) return;
  
  pollCount++;
  if (pollCount > MAX_POLL_COUNT) {
    console.log('[轮询] ⏰ 达到最大轮询次数，停止检测');
    ElMessage.warning('二维码已过期，请重新打开');
    stopPolling();
    return;
  }
  
  // ... 原有逻辑
};
```

---

### 4️⃣ 转移临时文件超时（30 秒）

**位置**: `FileUpload.vue` - `transferMobileFiles()`

**优化建议**:
```typescript
const transferMobileFiles = async (bizId: number): Promise<TempUploadFile[]> => {
  if (!currentTempToken.value || mobileUploadedFiles.value.length === 0 || !bizId) {
    console.error('转移临时文件失败：缺少必要参数');
    ElMessage.error('转移文件失败：缺少必要参数');
    return [];
  }

  const loadingMsg = ElMessage({
    message: '正在转移文件，请稍候...',
    type: 'info',
    duration: 0,
  });

  try {
    console.log('[文件转移] 🚀 开始转移临时文件...');
    
    const transferPromise = transferTempFiles({
      token: currentTempToken.value,
      bizType: props.bizType,
      bizId: bizId.toString(),
    });
    
    // 设置超时：30 秒
    const timeoutPromise = new Promise((_, reject) => {
      setTimeout(() => {
        reject(new Error('文件转移超时（30 秒）'));
      }, 30000);
    });
    
    const response = await Promise.race([transferPromise, timeoutPromise]) as any;

    if (response.code === 200 && response.data) {
      const transferredFiles = response.data;
      loadingMsg.close();
      ElMessage.success(`成功转移 ${transferredFiles.length} 个文件到业务`);
      
      // 刷新文件列表
      await loadFiles();
      
      return transferredFiles;
    } else {
      loadingMsg.close();
      ElMessage.error('转移临时文件失败');
    }
  } catch (error: any) {
    console.error('[文件转移] ❌ 转移失败:', error);
    loadingMsg.close();
    
    let errorMsg = '转移临时文件失败';
    if (error.message?.includes('超时')) {
      errorMsg = '文件转移超时，请检查网络后重试';
    }
    
    ElMessage.error(errorMsg);
  }
  
  return [];
};
```

---

## 📊 超时时间汇总表

| 步骤 | 超时时间 | 失败策略 | 友好提示 |
|------|----------|----------|----------|
| IP 检测 | 2 秒 | 使用后备 IP | "IP 检测失败，将使用默认配置" |
| 创建 Token | 10 秒 | 显示错误 | "操作超时，请检查网络连接后重试" |
| 文件轮询 | 30 分钟 | 停止轮询 | "二维码已过期，请重新打开" |
| 转移文件 | 30 秒 | 显示错误 | "文件转移超时，请检查网络后重试" |

---

## 🎯 完整的用户体验流程

### 正常流程：
```
1. 点击"手机扫码上传"
   ↓
   💬 "正在准备上传环境，请稍候..."
   ↓
2. 创建 Token（<10 秒）
   ↓
   💬 "上传环境准备成功"
   ↓
3. 检测 IP（<2 秒）
   ↓
4. 显示二维码
   ↓
   💬 "二维码有效期 30 分钟，过期后请重新打开"
   ↓
5. 手机扫码上传文件
   ↓
   💬 "手机上传了 X 个新文件"
   ↓
6. 关闭二维码弹窗
   ↓
7. 转移文件（<30 秒）
   ↓
   💬 "成功转移 X 个文件到业务"
   ↓
✅ 完成
```

### 异常流程：

#### 场景 1：IP 检测超时（>2 秒）
```
💬 "正在准备上传环境，请稍候..."
   ↓
⏰ IP 检测超时
   ↓
💬 "IP 检测失败，将使用默认配置，手机上传可能无法使用"
   ↓
💬 "上传环境准备成功"
   ↓
⚠️ 使用默认 IP：192.168.0.151
   ↓
💬 "二维码有效期 30 分钟"
```

#### 场景 2：创建 Token 超时（>10 秒）
```
💬 "正在准备上传环境，请稍候..."
   ↓
⏰ 创建 Token 超时
   ↓
💬 "操作超时，请检查网络连接后重试"
   ↓
❌ 弹窗关闭
```

#### 场景 3：二维码过期（>30 分钟）
```
💬 "二维码有效期 30 分钟，过期后请重新打开"
   ↓
⏰ 30 分钟后
   ↓
💬 "二维码已过期，请重新打开"
   ↓
❌ 轮询停止
```

#### 场景 4：文件转移超时（>30 秒）
```
💬 "正在转移文件，请稍候..."
   ↓
⏰ 转移超时
   ↓
💬 "文件转移超时，请检查网络后重试"
   ↓
⚠️ Token 保留，可以稍后重试
```

---

## 🔧 如何测试超时

### 测试 1：IP 检测超时
1. 打开浏览器开发者工具
2. 切换到 Network 标签
3. 禁用所有网络
4. 点击"手机扫码上传"
5. 观察：
   - ✅ 2 秒后显示"IP 检测失败"警告
   - ✅ 仍然显示二维码（使用默认 IP）

### 测试 2：创建 Token 超时
1. 使用网络限速工具（如 Chrome DevTools 的 Network Throttling）
2. 设置网速为 50kbps
3. 点击"手机扫码上传"
4. 观察：
   - ✅ 10 秒后显示"操作超时"错误
   - ✅ 弹窗自动关闭

### 测试 3：文件转移超时
1. 使用网络限速工具
2. 手机上传一个文件
3. 关闭二维码弹窗
4. 观察：
   - ✅ 30 秒后显示"文件转移超时"错误
   - ✅ Token 保留，可以稍后重试

---

## 📝 错误提示文案规范

### 加载中文案：
- ✅ "正在准备上传环境，请稍候..."
- ✅ "正在转移文件，请稍候..."

### 成功文案：
- ✅ "上传环境准备成功"
- ✅ "成功转移 X 个文件到业务"
- ✅ "手机上传了 X 个新文件"

### 警告文案（可继续）：
- ✅ "IP 检测失败，将使用默认配置，手机上传可能无法使用"
- ✅ "二维码有效期 30 分钟，过期后请重新打开"
- ✅ "二维码已过期，请重新打开"

### 错误文案（不可继续）：
- ✅ "操作超时，请检查网络连接后重试"
- ✅ "创建上传 Token 失败，请稍后重试"
- ✅ "文件转移超时，请检查网络后重试"

---

## 🎨 UI/UX 优化建议

### 1. 加载状态优化
```typescript
// 使用进度条或步骤指示器
const loadingSteps = ref([
  { title: '创建 Token', status: 'pending' },
  { title: '检测网络', status: 'pending' },
  { title: '生成二维码', status: 'pending' },
]);
```

### 2. 倒计时显示
```vue
<div class="qr-code-expire">
  <Icon icon="lucide:clock" />
  <span>二维码有效期：{{ formatTime(qrCodeExpireTime) }}</span>
</div>
```

### 3. 重试按钮
```vue
<ElButton 
  v-if="showRetry" 
  @click="retryLastStep"
  type="primary"
>
  重试
</ElButton>
```

---

## ✅ 总结

通过添加超时处理和友好的错误提示，手机扫码上传功能的用户体验得到了显著提升：

1. ✅ **IP 检测**：2 秒超时，自动降级
2. ✅ **创建 Token**：10 秒超时，友好提示
3. ✅ **文件轮询**：30 分钟有效期，过期提示
4. ✅ **转移文件**：30 秒超时，保留 Token

所有超时都有清晰的错误提示，用户知道发生了什么以及该如何操作！🎉
